# Environment configuration
# Set to 'local' for development, 'production' for production
NODE_ENV=local

# Server configuration
PORT=3000

# SSL/TLS configuration (only required for production)
# Uncomment and set these when NODE_ENV is not 'local'
# SSL_KEY_PATH=./privkey.pem
# SSL_CERT_PATH=./cert.pem
# SSL_CA_PATH=./chain.pem
